host: ${HOST:localhost}

server:
  port: ${PORT:8080}

spring:
  application:
    name: dd-docker-demo-web
    
#eureka:
#  client:
#    serviceUrl:
#      defaultZone: ${EUREKA_URL:http://user:password@localhost:5000}/eureka
#    fetch-registry: true
#  instance:
#    hostname: ${DOMAIN_NAME:localhost}
#    nonSecurePort: 80
#  password: ${EUREKA_USER_PASSWORD:password}